# ---------------------------------------------------------------------------
# Frontend environment. Copy to frontend/.env.
# Only NUXT_PUBLIC_* values reach the browser — never put a secret here.
# ---------------------------------------------------------------------------

# Base URL of the NestJS API, including its global prefix.
NUXT_PUBLIC_API_BASE=http://localhost:3001/api/v1
